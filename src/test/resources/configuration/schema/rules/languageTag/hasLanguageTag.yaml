format: json
fields:
  - name: id
    path:  $.['id']
  - name: description
    path: $.['description']
    asLanguageTagged: true
    rules:
      - hasLanguageTag: allOf
        debug: true
        id: 1.1
